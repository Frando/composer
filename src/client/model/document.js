sc.models.Document = function(document) {
  // Initialize document

  this.graph = new Data.Graph(document);

  this.insert = function(type, options) {

  };

  this.delete = function(node) {

  };

  this.move = function(node, options) {

  };
};
